# Forklore ProGuard/R8 rules.
# Empty by default: minification is off until M5. Add keep rules here when it
# is enabled, particularly for anything reflected over (Room entities are safe;
# kotlinx.serialization classes need their generated serializers kept).
