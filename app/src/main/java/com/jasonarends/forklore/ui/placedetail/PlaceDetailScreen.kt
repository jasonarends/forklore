package com.jasonarends.forklore.ui.placedetail

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Stub: issue #2 replaces this with the real detail screen. All this needs to do until then is
 * exist, so tapping a place in the list has somewhere to go.
 */
@Composable
fun PlaceDetailScreen(placeEntryId: String, modifier: Modifier = Modifier) {
  Text("Place detail coming soon.", modifier)
}
